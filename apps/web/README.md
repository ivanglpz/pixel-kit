# PixelKit Web

The current Next.js application still lives at the repository root.

This folder is reserved for the future move of the web app into `apps/web`. Keeping it as a placeholder lets the workspace shape exist without performing a large file move in the first migration phase.

